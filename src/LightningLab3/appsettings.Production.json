{
  // Production config — sensitive values come from environment variables, NOT this file.
  //
  // The following env vars must be set in docker-compose.yml (or your cloud host):
  //   ConnectionStrings__DefaultConnection  → PostgreSQL connection string
  //   RatingsApi__ApiKey                    → your real production API key
  //
  // This file intentionally contains no secrets.
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
